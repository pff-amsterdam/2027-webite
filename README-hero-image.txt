Drop the hero background photo here as:

  hero-background.jpg

It's referenced directly from HomeView.vue via the public path
"/hero-background.jpg", so no code changes or rebuild are needed —
just add the file (jpg or png; update the extension in HomeView.vue
if you use something other than .jpg).

Until the file is added, the page falls back to a solid navy
background color.
